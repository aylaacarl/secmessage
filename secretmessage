<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pesan Rahasia</title>
    <style>
        /* Mengatur latar belakang dan font */
        body {
            font-family: Georgia, serif;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            background-color: #2c3e50; /* Warna latar belakang */
        }

        /* Memusatkan dan mengatur judul */
        h1 {
            margin-top: 50px;
            color: white;
            text-align: center;
        }

        /* Kontainer utama untuk scroll */
        .container {
            max-width: 800px;
            margin: auto;
            padding: 20px;
            background-color: rgba(169, 169, 169, 0.9); /* Latar belakang pesan abu-abu */
            border-radius: 5px;
            color: black; /* Warna font pesan hitam */
            position: relative;
            z-index: 1;
        }

        /* Audio */
        audio {
            display: block;
            margin: 20px auto;
        }

        /* Tetapan hujan */
        .raindrop {
            position: absolute;
            top: -50px;
            width: 2px;
            height: 10px;
            background: rgba(255, 255, 255, 0.3); /* Tetapan lebih transparan */
            animation: fall linear infinite;
        }

        /* Animasi hujan */
        @keyframes fall {
            to {
                transform: translateY(100vh);
                opacity: 0; /* Membuat tetesan menghilang di akhir animasi */
            }
        }

        /* Overlay latar belakang untuk efek hujan */
        .rain-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }
    </style>
</head>
<body>
    <!-- Efek tetesan hujan di latar belakang -->
    <div class="rain-container" id="rain-container"></div>

    <!-- Kontainer utama yang dapat digulir -->
    <div class="container">
        <h1>This Little Secret Message for You</h1>
    
